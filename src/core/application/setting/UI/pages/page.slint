import { VerticalBox, ScrollView } from "std-widgets.slint";

export component Page inherits ScrollView {
    VerticalBox{
        alignment: start;
        padding: 10px;
        padding-left: 30px;
        @children
    }
}