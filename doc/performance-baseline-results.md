# 性能基线执行记录

2026-09-24，Rotor 3.0.2 开发身份、Windows release 构建。本记录是局部基线，不是两平台端到端性能验收。复现命令、测量口径和完成条件见 [采集说明](performance-baseline.md)。

## 环境

- CPU：Intel Core Ultra 7 265K；物理内存：68,044,115,968 字节。
- 磁盘：WD_BLACK SN7100 1TB、Samsung PSSD T7 Shield；未控制 OS 文件缓存。
- 原生 `display_info`：3840×2160，原点 (0, 0)，缩放 2；2160×3840，原点 (3840, -843)，缩放 2。
- 源码为未提交工作区。二进制、采集器和已跟踪差异的 SHA-256，以及 Git 状态随采样报告保存。空闲采样期间未并行进行 Rust 构建或测试；没有隔离系统和会话的全部后台负载。
- 两次空闲采样使用同一个 release 二进制。构建后还有不涉及已测场景的 CLI、搜索计时节点和测试调整；测量对应下列二进制哈希，不能把采集时的工作区差异哈希视为精确构建来源。

## Windows 合成像素准备

每条路径 2 次预热、30 次有效采样，历史双缓冲模拟与当前单缓冲实现交替执行。以下只列当前实现。

| 像素范围 | P50（ms） | P95（ms） | 理论长期像素占用（MiB） |
| --- | ---: | ---: | ---: |
| 1920×1080 | 0.970 | 1.039 | 7.91 |
| 3840×2160 | 3.423 | 9.276 | 31.64 |
| 7680×2160 | 4.880 | 8.553 | 63.28 |

180 个有效样本（3 档 × 2 路径 × 30 次），运行返回成功。4K P95 波动明显，不能从这一次测量推导稳定的优化收益。双 4K 是等像素单缓冲；表中不是实际进程峰值，也不包含截图采集、编码保存、OCR 或窗口呈现。

本地原始数据：`target/perf-baseline-20260924-pixels.json`。证据位于忽略的 `target` 目录，清理前应另行归档；仓库内的汇总不替代原始样本。

基准二进制 SHA-256：`2fc10c91047937370aaae59b722741fcc75a89c0d75f2578d2cdb83dbaff3a70`。
桌面二进制 SHA-256：`830e6a93541b965aabae5268aaca0f5983b23fe9e88588e1120077aa7f468e0e`。

## Windows 空闲资源

每次使用新合成 profile，先稳定 60 秒再采样 120 秒，无交互。CPU 百分比相对于一个逻辑核心，内存峰值仅指采样期间观测值。

| 场景 | CPU 平均 | 私有内存峰值 | 工作集峰值 | 有效资源样本 |
| --- | ---: | ---: | ---: | ---: |
| 索引关、快捷键关 | 0.0130% | 35.10 MiB | 51.51 MiB | 1089 |
| 索引关、开发快捷键开 | 0.0391% | 35.12 MiB | 51.48 MiB | 1091 |

两组均无采集失败、无应用 WARN/ERROR 日志。本地原始证据：`target/perf-baseline-20260924-idle/`、`target/perf-baseline-20260924-hotkeys/`（首轮采集器尚未自动保存 `source.diff` 和 `recorder.py`，仅记录哈希）。每组只有一次进程运行，不能从均值差异推导稳定的快捷键开销。不代表启用索引、交互负载、GPU 占用或正常退出的验收结果。

## 覆盖状态

| 项目 | Windows | macOS |
| --- | --- | --- |
| 合成像素准备 30 次原始样本 | 已采集 | 未执行 |
| 空闲基础对照（无索引、无快捷键） | 已采集 | 未执行 |
| 启用快捷键的空闲占用 | 已采集 | 未执行 |
| 启用索引的空闲占用 | 需专用合成系统 | 未执行 |
| 冷/暖搜索、小中大合成目录树 | 需专用合成系统及交互采样 | 未执行 |
| 原生截图、连续保存/OCR、恢复空闲 | 未执行 | 未执行 |
| GPU 内存、物理屏幕可见时间 | 未执行 | 未执行 |

当前会话只有 Windows 环境，未执行 macOS 编译或实机采样；Python 原生进程计数器测试已加入 Windows/macOS CI，尚无本次 macOS CI 结果。完整基线待办保持未完成。

## 实现验证

- Python 10 项测试通过：原生进程计数器、分位数、请求 ID 隔离、多屏缺失、轮转顺序、重复事件和丢失日志等。
- Windows `rotor-platform`、`rotor-ui`、`rotor-desktop` 相关测试通过；新增 UI 测试覆盖等待中、过期及被替换的搜索请求，不提前记首批绘制。
- 格式检查、上述三个 crate 的全部 target 严格 Clippy、共享核心依赖边界检查通过。
- 合成目录生成和日志汇总命令已实际执行。未执行完整工作区测试、macOS 编译或安装验收；本次没有修改截图像素、OCR、编码或资料持久化逻辑。
