# GPUI 迁移剩余任务汇总

## 当前执行范围（2026-09-08 用户调整）

**最新指令：跳过所有可视化测试，继续迁移。** 后续不再进行截图对照、人工窗口/IME/浮窗交互和视觉取样，不再等待此前 `short` 人工辅助结果。已有证据保留，未执行的可视化项目标为“按用户要求跳过”，不计为通过。macOS 任务继续暂缓。

执行顺序相应调整为：完成共享代码与资源的最终目录迁移，运行 Windows 编译、自动测试、依赖边界和打包/数据恢复检查，再推进剩余工程切换。可视化门槛不再阻挡工程迁移；非可视化的安装恢复、签名、版本递增及发布验证仍单独记录状态。六个共享 crate 已移至 `crates/`、资源已移至 `assets/`，原生与旧壳适配入口同步；workspace 174 项测试及原生严格检查通过，见[工程迁移记录](evidence/windows-root-layout.md)。新布局的 release/打包正在继续核验。

用户现已恢复实机验收授权。最新 [Windows 设置实机记录](evidence/windows-settings-live.md) 已完成普通中英文自动保存、只读失败/关闭保留、解除故障重试、明确放弃并重开、局部快捷键校验/录制，以及当前中文输入法组字确认/取消的一组操作。非法快捷键错误已本地化并在新 release 窗口复验中英文提示，另取得深色/英文及 496×401 小窗口设置样本；未将这些局部结果视为 UI-01–UI-05 或 V17 全部通过。

在此前“先继续无窗口检查”阶段，按 `6efe337` 重新生成 development/production Windows 安装包，包含近期贴图和快捷键恢复修复，通过中文/空格暂存路径、外部工作目录资源定位、身份与哈希检查；见[最新候选](candidate-status.md)。较早 `fd9b206` 候选另有文件篡改/过期源码收据拒绝证据。这批候选准备没有执行安装；这些证据不关闭 UI、IME 或 G8/G9。

后续无窗口核对修复了“贴图保存失败后普通关闭，误记住失败目录”的状态串用问题，见[功能核对](evidence/windows-feature-audit.md)。UI 25 项测试及 native/UI 严格 clippy 通过；此改动已进入 `6efe337` 候选，实际失败交互仍待验收。

另修复快捷键回滚本身失败后，注册清单与后端状态不一致、后续保存无法补注册或清理的问题；[故障复现与恢复记录](evidence/windows-shortcut-recovery.md)包含修复前失败、修复后通过的测试。runtime 33 项、desktop 4 项及严格 clippy 通过；真实系统热键冲突验收仍未关闭。

普通文本、排除目录及快捷键自动保存已接入，并补齐回执隔离、组字保护、失败草稿和关闭/退出等待（[记录](evidence/windows-autosave.md)）。后台包含真实 Windows 文件占用下的整批失败/回滚与解除占用重试；后续修复后 runtime 34 项测试通过，UI 已有 25 项测试通过。已取得上述部分真实输入/关闭证据，UI-04 完整验收仍未关闭。

翻译失败路径继续见 [流式处理核验](evidence/windows-translation-stream.md)：已修复 DeepSeek 提前 EOF 和异常结束被当作成功的问题，回归测试通过；真实窗口/三引擎联合验收仍未关闭。

已增加真实 localhost 请求的替换/取消/503 错误测试，并为原生搜索补齐索引构建、加载、释放和失败反馈。无窗口检查通过，尚不作为 GUI/IME 验收完成的依据。

44 项功能对照已逐行回填实现位置、主要 commit 和证据边界。本次核对发现并修复了 F04 搜索窗关闭未发 release、后台结果缓存未释放的问题（[回归记录](evidence/windows-search-release.md)）。额外 redo 入口已按 P6 首发范围移除；快捷操作无草稿时的启停/删除已直接事务保存，普通文本/快捷键也已接入自动保存。UI-04 仍需真实交互收口，详见 [功能核对](evidence/windows-feature-audit.md) 和 [自动保存记录](evidence/windows-autosave.md)。

最新 Windows 续作见 [续作核验](evidence/windows-ui-followup.md)：上轮合成贴图未出现已定位为测试记录的 `image_rect` 原点错误；修正夹具通过真实 PinStore 只读检查，窗口恢复验收仍待完成。

概览索引详情及后台警告的即时展示已补齐，13 项 UI 测试、严格 clippy、release 构建和 workspace 格式检查通过。用户授权后已恢复部分窗口核验，确认单张合成贴图恢复可见，并采集两屏 200% DPI 的真实拓扑与翻译空输入截图；随后用户再次以 Esc 停止桌面操作。翻译结果、完整贴图操作和其余 Windows 验收仍未关闭。

用户明确要求“暂时不完成 MacOS 相关的任务，专注于 Windows”。本轮推进 Windows UI、交互、安装更新恢复、性能及满足 Windows 门槛后的工程收尾；macOS 构建、桌面、分发与性能项标为暂缓，不以 Windows 结果代替。跨平台完整 G0–G9 状态继续单独保留，不能把暂缓写成通过。最终删除旧壳或切换发布渠道时仍应说明对尚未验收 macOS 的影响。

核对日期：2026-09-08。代码基准：`207e1bf`；依据为当前源码、[实施进度](progress.md)、[候选记录](candidate-status.md) 和 [验收要求](validation.md)。本次只整理文档，没有重新运行构建、实机测试或远端 CI；下文已有测试结果均引用历史记录。

**主要业务功能已接入原生壳，但完整迁移尚未完成。首先完成 UI 现代化美化，再推进双平台验收、安装更新与恢复演练、性能基线及 P9 工程清理和正式发布切换。** 不能把早期计划中全部未勾选的任务或功能表里的“待迁移”理解为所有功能仍未实现，也不能把代码完成视为 G0–G9 已通过。

## 0. 第一优先级：UI 现代化美化（先于其余剩余任务）

**验收调整：以下可视化测试按最新用户指令跳过，保留历史要求和已取得证据，不再作为后续工程工作的等待条件。** 已实现的 UI 和已发现的功能缺陷仍保留并继续维护。

2026-09-08 用户新增要求：**参考旧 UI 完成原生 UI 现代化美化，美观度和实用性均不得低于旧 UI。** 此任务排在所有剩余任务之前，所需旧 UI 取样与交互核对作为本任务的首步；不能只达到“功能可用”就进入后续收尾。

- [ ] **UI-01：建立旧 UI 对照。** 结合旧 Vue 页面、组件、主题样式和实际界面，整理各窗口的截图与典型操作流程，记录布局、信息密度、视觉层级、常用入口、快捷键和反馈方式。参考范围包括设置/概览/快捷操作/更新、搜索、翻译、截图遮罩、贴图工具栏、标注和 OCR。
- [ ] **UI-02：统一现代化视觉规范。** 在 GPUI + gpui-component 中统一字体层级、间距、对齐、颜色、图标、圆角、边框及阴影，覆盖浅色/深色/跟随系统、中英文和不同 DPI；保留旧 UI 有效的信息组织和操作习惯。
- [ ] **UI-03：逐模块完成界面美化。** 按上述完整范围落实布局和控件优化，补齐悬停、选中、焦点、禁用、加载、空结果、错误及成功反馈；长文案、窄窗口和缩放下关键内容与操作不能被遮挡或截断。
- [ ] **UI-04：保证实用性不退步。** 常用操作入口清楚、步骤不多于旧 UI，键盘操作和中文 IME 可用；搜索结果易扫描，翻译结果易阅读复制，截图/贴图工具不遮挡关键内容，设置分组和保存反馈明确。美化不能牺牲功能覆盖、对比度、点击区域、响应速度或低占用目标。
- [ ] **UI-05：完成新旧对照验收。** 使用相同内容、窗口尺寸、主题和 DPI，逐模块保存新旧截图与操作记录，核对视觉一致性、层级、留白、可读性及操作效率。逐项记录改进和差异，修复视觉或使用体验退步后再关闭本任务；编译通过、静态设计稿或单张首页截图不能代替实际界面验收。

交付：实际原生 UI 改动、统一视觉规范、逐模块新旧对照及验收记录，记录放在本目录 `evidence/` 下。视觉验收必须满足“美观度不低于旧 UI”，交互验收必须满足“实用性不低于旧 UI”；本任务通过后仍需完成后续双平台完整验收与性能测试。

2026-09-08 续作：已建立[源码对照与视觉规范](evidence/ui-modernization.md)，并实现设置左导航、主题卡片、固定保存区、概览和快捷操作第一批美化，native check/debug build 通过。实际窗口取样被用户 Esc 停止，尚未完成视觉验收；UI-01–UI-05 保持未关闭。

后续续作已完成搜索/翻译/截图/贴图与 OCR 第一批美化，取得旧壳七类实际截图及部分原生截图，并据旧 UI 修正主题、快捷键密度、快捷操作列表、引擎参数显示和翻译窗口高度。原生/共享业务测试与严格 clippy 通过；最后一轮桌面操作再次被用户 Esc 停止。原生概览索引详情、合成贴图恢复未出现的诊断、其余模块和同尺寸/DPI 对照仍待完成，详见 UI 证据。

## 1. 已有实现与验收边界

| 范围 | 已有实现或证据 | 仍未完成 |
|---|---|---|
| P1 核心解耦 | 根 workspace、独立业务核心、配置事务、事件桥、请求身份与取消；两目标平台核心依赖边界检查 | 双平台完整构建和旧壳兼容证据收口；最终目录搬迁在 P9 |
| P2–P4 常驻与工具 | 托盘、单实例、提权、自启动、设置、快捷键录制与事务、快捷操作、搜索、输入/划词翻译 | 原生窗口、系统事件、中文输入和失败路径联合验收 |
| P5–P7 截图与贴图 | 多屏会话、框选/取色、贴图恢复/裁剪/标注/保存复制、OCR；Windows 合成 OCR 与空闲释放已有证据 | 多屏/DPI、实际画布与 OCR 交互、导出兼容及压力验收 |
| P8 交付与更新 | 独立 updater、双身份构建、Windows NSIS、macOS 打包/交接代码、备份和恢复逻辑、候选 CI | Mac 构建运行、真实安装/升级/卸载/故障恢复、候选签名及渠道联调 |
| P9 最终切换 | 已有部分导出时序修复和候选来源校验 | 性能验收、业务与资源搬迁、旧前端/Tauri 删除、正式发布流程和文档切换 |

历史 Windows 候选记录为 125 项 native/core 测试通过，版本仍为 `2.6.0`，未使用真实更新私钥签名或发布。macOS 依赖图检查不等于 macOS 编译、打包或运行通过。

初次核对时，`crates/rotor-desktop/src/capture.rs` 的 Windows 遮罩视口/DPI 缓存修正尚未提交，后已收入 `3cb21b6`。当前 `6efe337` 候选包含该修正及后续 UI/自动保存实现，已登记来源摘要；真实多屏交互仍按上述验收边界保留。

## 2. 已实现但尚待验收的任务

以下复选框代表该任务的完整验收尚未关闭，不表示其中所有子场景从未测试。每项结果按 Windows x64 / macOS arm64 分别记录“通过、失败、未测”，失败发现的代码缺口继续修复。

### A. 旧版基线和平台支持范围（P0 / G0）

- [ ] 补齐旧版搜索、三种翻译引擎、截图/贴图/OCR、设置和快捷操作的截图或操作记录，以及用于兼容验证的贴图副本。目前主要使用合成 fixture。
- [ ] 冻结旧版 `zoom × DPI` 导出矩阵：同一输入在 zoom 50/100/200%、DPI 100/150/200% 下记录尺寸与像素，并与原生保存、剪贴板及 OCR 输入比较。固定尺寸的 P0 PNG 不能代替该基线。
- [ ] 确认最终承诺的最低 Windows/macOS 版本；补 macOS arm64 release 构建、Mach-O 最低系统信息及目标系统实际启动结果。
- [ ] 补复杂中文/字体回退、颜色与透明度、GPU 不可用启动提示等证据；登记需要保留的行为差异。

依据：[依赖基线](dependency-baseline.md)、[旧版基线](baseline.md)。早期文档中“旧包尚未下载/验签”的描述已被后续 P8 证据更新：两端旧 v2.6.0 产物已验签，这项不再列为待办。

### B. Windows/macOS 原生功能联合验收（P2–P7 / V01–V17）

| 待完成任务 | 重点场景 | 验收编号 |
|---|---|---|
| [ ] 常驻、窗口与实例 | 无窗口常驻、100 次唤起/关闭、托盘退出、普通/提权启动、UAC 取消、二次启动、旧新实例互斥；macOS Dock、Spaces/fullscreen、焦点返回 | V01–V03 |
| [ ] 系统服务与设置 | 默认/自定义热键、录制、冲突/落盘失败回滚、睡眠恢复；自启动登录重启、LaunchAgent、原生对话框、中文/空格路径；中英/主题跟随及概览 | V04–V07 |
| [ ] 搜索与翻译 | 固定搜索语料、真实索引/分页/打开文件、快速输入旧结果隔离；三种引擎流式/失败反馈、窗口位置/高度、划词恢复及并发用户复制 | V08–V09 |
| [ ] 截图会话与多屏 | 混合 DPI、负坐标、屏幕四角、跨屏焦点、连续截图/取消/恢复、迟到回调、热插拔/旋转/睡眠、权限拒绝和资源不足 | V10–V12 |
| [ ] 贴图、标注与导出 | 两张/十张贴图、跨屏拖动缩放、八方向裁剪、中文标注/撤销；连续保存/复制、保存框取消、磁盘满/剪贴板占用、成功关闭和失败保留 | V13–V14 |
| [ ] 持久化与 OCR | 有/无 image_rect、显示器缺失、坏记录/缺图、快速写入后退出恢复；字框定位、拖选/复制、空结果/缺模型、旧结果失效、空闲释放后重建 | V15–V16 |
| [ ] 输入与焦点收尾 | Windows 微软拼音/macOS 拼音的确认/取消、Emoji/混合标点、Tab 导航；文字编辑时 S/H/Enter/Escape、模态框失焦、跨模块连续操作 | V17 |

Windows P0 原型已有部分实机证据，但不能覆盖正式应用所有场景；macOS 目前没有记录到可用的构建/实机验收结果，需要 Mac 或对应 CI 执行构建，并在真实桌面补窗口、权限、剪贴板和 OCR 测试。

### C. 安装、更新与数据恢复闭环（P8 / G8 / V18–V20）

- [ ] 在隔离测试环境执行 Windows 首次安装、覆盖升级、卸载与自启动清理；核对安装身份、中文/空格路径、卸载保留资料和用户新增文件。
- [ ] 在 macOS 完成 app/DMG/更新归档构建和实际安装；验证资源、权限、应用替换与重启、隔离属性及实际分发方式。更新签名与 Apple 代码签名/Gatekeeper/公证分别记录结果。
- [ ] 实测旧 `v2.6.0 → GPUI`、`GPUI N → N+1` 两条更新链，兼顾未经修改的旧客户端/安装器与测试端点等价构建，说明兼容证据的边界。
- [ ] 注入断网、坏签名、错误 target、下载取消、UAC 取消、无写权限、磁盘满、解包/辅助进程失败，确认可重试且旧程序及数据可恢复。
- [ ] 验证 Windows/macOS 新版本启动失败回退。Windows 当前普通启动错误路径已有实现，装载失败、崩溃和挂起仍需独立验证；根据结果补足恢复代码，不能预先声称已覆盖。
- [ ] 用实际旧版与原生安装程序完成数据副本双向读写和回退：保留未知字段、裁剪/缩放/位置及迁移后新截图。已有合成数据与 legacy Rust 读取器测试不能代替安装版 GUI 演练。
- [ ] 选择可用于递增升级的候选版本，重新生成双平台、匹配身份的构建/资源收据和产物清单，运行候选工作流并完成真实更新签名验证。当前同为 2.6.0 的包不能证明版本递增更新成功。
- [ ] 联调 GitHub/Gitee 元数据、附件字节/签名与源不可用处理；演练最低 OS 路由及旧 macOS 客户端分流，确认不向不兼容系统提供新包。

依据：[候选记录](candidate-status.md)、[更新交接](update-handoff.md)、[更新与恢复验收步骤](validation.md#6-更新安装和恢复演练)。候选工作流已有构建/可选签名能力，但仅上传审查产物；实际签名、远端运行和发布联调不能据 workflow 文件存在记为通过。

### D. 性能基线与资源收敛（P9-01 / V21）

- [ ] 在同一硬件、固定语料/图片和 release 安装产物上重新测旧版与 GPUI；旧版计入 WebView 子进程，冻结预算。已有探索性待机样本不作为性能结论。
- [ ] 补 20 次冷启动、10 次预热后至少 100 次交互、稳定 60 秒后 120 秒待机采样及至少 2 小时常驻曲线。
- [ ] 测搜索/翻译热唤起、查询首批结果、截图首帧、标注帧耗时、OCR 冷热和流式翻译；拆分捕获、纹理上传、推理及 UI 时间。
- [ ] 测双屏/十贴图峰值，以及 100 次截图/创建/关闭后的 CPU、内存、GPU 纹理、句柄和引用回落；修复持续增长、积压或回归。
- [ ] 按 [性能预算](validation.md#4-性能测量与目标) 判定通过：例如待机 CPU ≤ 单逻辑核 0.5%、工具窗口热唤起 p95 ≤ 150ms，并满足相对旧版的约束；未达到优化目标需记录原因。

## 3. 尚待实施的最终工程切换（P9 / G9）

最新用户指令取消可视化测试等待。先实施可逆的目录/构建迁移并保持兼容检查；安装恢复、非可视化性能、正式发布和旧壳删除的状态分别追踪，不把跳过的可视化验收记成通过。

- [ ] **P9-02：搬迁业务与资源。** 六个共享 crate 已移至根 `crates/`，资源已移至根 `assets/`，workspace、资源定位、构建收据、xtask、测试及 CI 检查路径已同步，174 项测试通过；完成新布局 release/打包核验后收口。安装配方保留 `native/`，目录说明已同步。
- [ ] **P9-03：移除旧壳和前端。** 删除不再使用的 Vue/TypeScript/Vite/前端依赖、Tauri 壳与 integration、插件/配置/capability 和 WebView bridge；先保留仍用的业务、资源、测试及许可证。
- [ ] **P9-04：切换正式构建发布和开发文档。** 当前 `.github/workflows/publish.yml` 仍为 Tauri + Node/Yarn 发布流程，需改为原生流程；消除旧版本镜像/脚本依赖，更新中英文 README、AGENTS.md、贡献与平台要求、资源/签名/恢复说明。
- [ ] **P9-05：最终依赖与干净环境验证。** 重新执行完整 workspace 格式、check/test/clippy/release 及两端打包检查，收敛历史 lint；在无 Node/Yarn 环境构建，审计最终 normal/build 依赖与产物不含 Tauri/WebView/JS 运行路径。
- [ ] **P9-06：候选验收收口。** 功能表逐行补实现位置/commit、两端证据和行为差异；输出版本/源码/包哈希、支持系统、已知限制、升级兼容表和可操作的安装/数据恢复步骤，之后才关闭 G9 和切换正式更新渠道。

## 4. 执行顺序和文档收口

1. 按用户要求跳过剩余可视化测试，保留已取得的实机证据；macOS 暂缓。
2. 完成共享代码、资源及构建入口迁移，运行 Windows 自动测试、格式、依赖和打包检查。
3. 推进 Windows 可自动执行的安装/升级/故障恢复、数据兼容和性能检查，继续记录实际证据。
4. 完成剩余旧壳清理、原生发布流程和开发文档；正式渠道切换前分别核实非可视化发布条件。
5. 对最终工程与产物复验，明确列出“通过、按用户要求跳过、暂缓、尚未完成”，再判定当前授权范围是否完成。

文档仍需随最终验收收口：[功能对照](feature-parity.md) 已回填 44 项代码位置、commit 和当前证据边界，验收未完成项保持未关闭；[迁移计划](README.md) 的阶段复选框不能仅凭代码存在勾选。`progress.md` 是追加历史，较早“下一步/尚未接入”可能已被后文完成。`validation.md` 已同步根 workspace 和实际 xtask CLI；后续继续用其证据模板记录结果，不以本汇总代替验收。
