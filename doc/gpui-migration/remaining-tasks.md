# GPUI 迁移剩余任务汇总

## 当前状态与标记规则

核对日期：2026-09-09。本轮已继续执行工程检查和 Windows 非可视化安装验收，候选版本递增为 `2.6.1-rc.1`。历史 `0dd6792`/`3a07780` 的 2.6.0 产物记录保留，不冒充当前候选。

- `[x]` 表示该条明确限定的实现、检查或交付已完成，并附对应证据；代码完成不等于全部平台/场景验收通过。
- `[ ]` 表示该条仍有未完成工作；大项已拆成可独立勾选的子项，避免已完成工作继续混在未勾选的整项中。
- **跳过**：用户要求跳过全部后续可视化测试，包含截图对照、人工窗口/IME/浮窗操作和视觉取样；已有实机证据保留，不把跳过记成通过。
- **暂缓**：macOS 构建、运行、分发与性能验收；不以 Windows 结果替代。
- **旧文件清理完成（2026-09-09）**：重新授权后逐项删除 91 个旧文件；保留历史源码、资源和证据。[清单](evidence/legacy-removal-plan.md)

**旧文件物理清理已完成；开发身份递增安装、自启动项清理和一项待机预算已取得新增证据。正式身份隔离验收、完整性能矩阵及实际发布推广尚未收口，G8/G9 不记为全部通过。**

### 2026-09-09 分阶段续作

- [x] `e968779`：按清单删除 91 个旧文件；旧公钥测试证据迁入保留目录。删除后的格式、check、workspace 测试、严格 clippy、字体及依赖边界检查通过。
- [x] `ec802cd`：以干净工作树使用版本工具选择 `2.6.1-rc.1`，只更新 workspace 版本及锁文件，未打标签或推送。
- [x] `e992a80`：真实 localhost socket 验证 HTTP 503、截断、错误签名字节、超大响应、请求中取消及成功重试；失败不覆盖已有产物且清理临时文件。测试使用公开签名向量，正式 HTTPS/公钥约束保留。
- [x] `9fa21cf`：开发身份 `2.6.0 → 2.6.1-rc.1` 实际安装、旧程序备份、占用失败保留、同版替换、自启动项清理、卸载保留合成资料均通过。[安装记录](evidence/windows-incremental-rc1-2026-09-09.json)
- [x] `89311a8`：独立 Windows 更新启动监控随包交付；装载错误、提前退出、无就绪回执超时进入现有回退。真实子进程覆盖退出、挂起、发回执后立即退出及正常就绪；独立安装目录的损坏 PE → 备份恢复 → 后台重启 → 卸载实际通过。[恢复记录](evidence/windows-loader-recovery-rc1-2026-09-09.json)
- [x] `89311a8` 最终源码的 Windows 双身份 release/NSIS、库存哈希、身份与资源诊断、相同源码收据通过；开发包安装与恢复复验通过，正式包仅构建/诊断，未安装。[候选清单](evidence/windows-final-candidates-rc1-2026-09-09.json)、[完整边界及一次旧安装器失败记录](evidence/windows-nonvisual-rc1.md)
- [x] 安装版空资料、禁用索引和热键、后台常驻；稳定 60 秒后采样 120 秒，单逻辑核 CPU 平均约 0.051%，满足该限定场景的 0.5% 绝对预算。[原始样本](evidence/windows-idle-rc1-2026-09-09.json)
- [ ] 完整性能验收仍未完成：没有以该样本替代旧版进程树对照、交互/GPU 指标、20 次冷启动或 2 小时负载曲线。
- [ ] 远端候选/签名/发布受环境条件限制：`gh auth status` 显示未登录；当前无签名环境变量。已请求用户在本机配置，不在聊天中收取密钥。
- [ ] 正式身份隔离验收：本机已存在正式 Rotor 安装，未修改其安装、注册项或资料。需要无既有正式安装的隔离 Windows 环境。

## 0. UI 实现与已完成的局部实机检查

### UI-01–UI-03：已完成的对照准备与界面改造

- [x] 建立旧源码对照与视觉规范，取得旧壳七类实际截图及部分原生截图。[UI 记录](evidence/ui-modernization.md)
- [x] 完成设置左导航、主题卡片、固定操作区、概览和快捷操作首批美化。
- [x] 完成搜索、翻译、截图遮罩、贴图工具栏、标注和 OCR 首批美化；据旧界面修正主题、快捷键密度、快捷操作摘要、引擎参数显示和翻译窗口高度。
- [x] 补齐概览/搜索索引详情、构建/加载/释放/失败状态及后台警告即时展示；调整字体层级。[续作记录](evidence/windows-ui-followup.md)
- [x] 在 Windows 验证设置页深色/英文即时切换与落盘；取得 496×401 小窗口样本，确认通用页可操作、快捷键可滚动至末项、固定保存/关闭区可见。[实机记录](evidence/windows-settings-live.md)

### UI-04：已完成的实用性修复与检查

- [x] 普通文本、排除目录和快捷键接入自动保存；合并未消费的自动批次、保留显式提交顺序和请求回执隔离。`1737249`、`a225b02`。[自动保存记录](evidence/windows-autosave.md)
- [x] 实现组字保护、规范化回写保护、失败草稿与未保存标记，以及关闭/退出等待；排除目录重建在壳层合并触发。
- [x] 实际验证普通中英文输入自动保存；只读故障时保留草稿和窗口，解除故障后重试成功；明确放弃后关闭并重开，未保存内容未落盘。
- [x] 实际验证局部快捷键手输确认前不提交、非法组合确认后拒绝、F8 录制后自动保存；此场景禁用了系统全局注册，不代表全局冲突验收通过。
- [x] 实际验证当前中文输入法的组字文本不落盘、空格确认后保存、Escape 取消后保留窗口和已保存值；未独立确认具体 IME 产品身份，也未覆盖完整输入法矩阵。
- [x] 本地化非法快捷键提示，使用字段名称和操作说明，并在新 release 窗口复验中英文；其他字段的失败状态不会被语言设置保存成功掩盖。`0253418`。[上述实机证据](evidence/windows-settings-live.md)
- [x] 快捷操作无草稿时启停/删除直接事务保存；有其他草稿或编辑器时不隐式提交，取消编辑只恢复当前项。`35c6c52`。[功能核对](evidence/windows-feature-audit.md)
- [x] 按 P6 首发范围移除额外 redo 按钮及 Ctrl/Cmd+Shift+Z，保留撤销和内部失败恢复。`e9a298b`。
- [x] 修复贴图保存失败后普通关闭误记住失败目录的问题，将目录绑定到对应导出请求。`5a138cc`。代码/自动检查完成，实际失败交互未另行通过。

### 尚未执行的完整可视化范围

| 验收编号 | 当前状态与保留边界 |
|---|---|
| UI-01 | **剩余对照跳过**：全部窗口的完整旧新截图/操作矩阵未补齐。 |
| UI-02 | **剩余视觉验收跳过**：不同主题、语言和 DPI 的完整规范核对未通过。 |
| UI-03 | **剩余视觉验收跳过**：全部模块的窄窗口、长文案及所有反馈状态未逐一验证。 |
| UI-04 | **剩余人工交互跳过**：保留上列局部实机通过项，不扩大为全功能/输入法/全局热键通过。 |
| UI-05 | **完整新旧对照跳过**：没有宣称整体美观度和实用性已完成对照验收。 |

## 1. 已完成的业务接入与可靠性工作

下列是实现范围；各场景的完整验收状态继续在第 2 节单列。[实施进度](progress.md)、[44 项功能对照](feature-parity.md)保存详细位置与历史记录。

- [x] **P1 核心解耦**：根 Cargo workspace、独立业务核心、配置事务、事件桥、请求身份与取消机制；共享核心不依赖 Tauri、GPUI 或 WebView。
- [x] **P2–P4 原生功能接入**：托盘、单实例、提权、自启动、设置、快捷键录制/事务、快捷操作、搜索和输入/划词翻译。
- [x] **P5–P7 截图与贴图接入**：多屏会话、框选/取色、贴图恢复/裁剪/标注/保存/复制、持久化与 OCR。
- [x] **P8 分发/恢复代码接入**：独立 updater、双身份构建、Windows NSIS、macOS 打包/交接实现、资料备份与恢复、候选 CI；macOS 代码存在不代表 Mac 验收完成。
- [x] 修复搜索窗关闭未释放索引及后台结果缓存未释放，补齐回归检查。`75429c5`。[搜索释放记录](evidence/windows-search-release.md)
- [x] 修复 DeepSeek 流提前 EOF、异常结束被当作成功；增加真实 localhost 请求的替换、取消及 HTTP 503 回归。`9928c12`、`4dadf59`。[翻译流记录](evidence/windows-translation-stream.md)
- [x] 通过真实 Windows 文件占用验证自动保存整批失败、回滚请求、内存/磁盘旧值保留及解除占用后重试。`beedc8b`。[记录](evidence/windows-autosave.md)
- [x] 修复快捷键回滚本身失败后的注册清单失真，重试能恢复缺失键/清理残留键，并使旧排队事件失效；故障测试先失败、修复后通过。`6efe337`。[恢复记录](evidence/windows-shortcut-recovery.md)
- [x] 用暂存字体/模型完成无窗口中英文 OCR，并确认 idle reaper 释放模型缓存。[工程记录](evidence/windows-root-layout.md)
- [x] 合成资料完成旧格式 → native 写回 → legacy Rust 读取器 → 二次导入，保留未知字段、其他工作区、裁剪/缩放/位置及备份。此项不替代安装版 GUI 双向演练。

## 2. 平台、安装与性能验收：已完成部分及剩余项

以下混合大项保留未完成的完整要求；视觉部分按用户指令跳过，macOS 部分暂缓，未验证的非可视化条件继续未勾选。

### A. 旧版基线和平台支持范围（P0 / G0）

- [x] 下载并核验旧 v2.6.0 Windows 安装包及 macOS 更新归档的现有公钥签名。属于历史产物核验，不代表 macOS 原生迁移验收通过。[记录](candidate-status.md)
- [x] 制作合成贴图夹具，修正 image_rect 原点，使用真实 PinStore 只读校验；保留单张贴图恢复可见的用户确认及两屏 200% DPI/负坐标拓扑记录。[记录](evidence/windows-ui-followup.md)

- [ ] 补齐旧版搜索、三种翻译引擎、截图/贴图/OCR、设置和快捷操作的截图或操作记录，以及用于兼容验证的贴图副本。目前主要使用合成 fixture。
- [ ] 冻结旧版 `zoom × DPI` 导出矩阵：同一输入在 zoom 50/100/200%、DPI 100/150/200% 下记录尺寸与像素，并与原生保存、剪贴板及 OCR 输入比较。固定尺寸的 P0 PNG 不能代替该基线。
- [ ] 确认最终承诺的最低 Windows/macOS 版本；补 macOS arm64 release 构建、Mach-O 最低系统信息及目标系统实际启动结果。
- [ ] 补复杂中文/字体回退、颜色与透明度、GPU 不可用启动提示等证据；登记需要保留的行为差异。

依据：[依赖基线](dependency-baseline.md)、[旧版基线](baseline.md)。早期文档中“旧包尚未下载/验签”的描述已被后续 P8 证据更新：两端旧 v2.6.0 产物已验签，这项不再列为待办。

### B. Windows/macOS 原生功能联合验收（P2–P7 / V01–V17）

实现和已通过的局部检查见第 0、1 节。下表保留完整验收范围；其中尚未执行的可视化/人工交互部分已按用户要求跳过，macOS 部分暂缓，其他未验证条件不计为通过。

| 待完成任务 | 重点场景 | 验收编号 |
|---|---|---|
| [ ] 常驻、窗口与实例 | 无窗口常驻、100 次唤起/关闭、托盘退出、普通/提权启动、UAC 取消、二次启动、旧新实例互斥；macOS Dock、Spaces/fullscreen、焦点返回 | V01–V03 |
| [ ] 系统服务与设置 | 默认/自定义热键、录制、冲突/落盘失败回滚、睡眠恢复；自启动登录重启、LaunchAgent、原生对话框、中文/空格路径；中英/主题跟随及概览 | V04–V07 |
| [ ] 搜索与翻译 | 固定搜索语料、真实索引/分页/打开文件、快速输入旧结果隔离；三种引擎流式/失败反馈、窗口位置/高度、划词恢复及并发用户复制 | V08–V09 |
| [ ] 截图会话与多屏 | 混合 DPI、负坐标、屏幕四角、跨屏焦点、连续截图/取消/恢复、迟到回调、热插拔/旋转/睡眠、权限拒绝和资源不足 | V10–V12 |
| [ ] 贴图、标注与导出 | 两张/十张贴图、跨屏拖动缩放、八方向裁剪、中文标注/撤销；连续保存/复制、保存框取消、磁盘满/剪贴板占用、成功关闭和失败保留 | V13–V14 |
| [ ] 持久化与 OCR | 有/无 image_rect、显示器缺失、坏记录/缺图、快速写入后退出恢复；字框定位、拖选/复制、空结果/缺模型、旧结果失效、空闲释放后重建 | V15–V16 |
| [ ] 输入与焦点收尾 | Windows 微软拼音/macOS 拼音的确认/取消、Emoji/混合标点、Tab 导航；文字编辑时 S/H/Enter/Escape、模态框失焦、跨模块连续操作 | V17 |

Windows P0 原型已有部分实机证据，但不能覆盖正式应用所有场景；macOS 目前没有记录到可用的构建/实机验收结果，需要 Mac 或对应 CI 执行构建，并在真实桌面补窗口、权限、剪贴板和 OCR 测试。

### C. 安装、更新与数据恢复闭环（P8 / G8 / V18–V20）

- [x] 修复新安装目录被 NSIS GetFullPathName 清空的问题，改用 Windows 路径规范化并检查失败/截断；静默错误不等待对话框，支持 UTF-16LE `/LOG` 日志。`7d2b59f`。[记录](evidence/windows-silent-install.md)
- [x] 在新的中文/空格目录完成开发身份静默首装，核对安装注册项、开始菜单、安装文件大小/哈希和安装目录内的身份/资源诊断。
- [x] 拒绝非空无关目录且保留原文件；旧程序被占用时覆盖失败且哈希不变；解除占用后同版本替换成功，旧安装和用户新增文件保留在备份中。
- [x] 完成静默卸载，清除程序、包内文件、开发注册项及快捷方式；保留合成资料、用户新增文件和升级备份。以上实际安装结果对应 `7d2b59f`，没有重复算作后来候选的安装验证。[完整记录](evidence/windows-silent-install-2026-09-08.json)
- [x] 将开发身份静默安装检查接入候选 workflow，默认 Windows，保留日志/结果；完整签名元数据仍要求双平台输入。仅代表工作流代码和本地脚本验证完成。
- [ ] 完成正式身份安装、递增版本升级和登录自启动清理；已有开发身份同版本测试不能替代。
- [x] 开发身份递增升级及卸载删除其自身自启动值（2026-09-09）；未执行登录重启，不替代上一条正式身份验收。
- [ ] 在 macOS 完成 app/DMG/更新归档构建和实际安装；验证资源、权限、应用替换与重启、隔离属性及实际分发方式。更新签名与 Apple 代码签名/Gatekeeper/公证分别记录结果。
- [ ] 实测旧 `v2.6.0 → GPUI`、`GPUI N → N+1` 两条更新链，兼顾未经修改的旧客户端/安装器与测试端点等价构建，说明兼容证据的边界。
- [ ] 注入断网、坏签名、错误 target、下载取消、UAC 取消、无写权限、磁盘满、解包/辅助进程失败，确认可重试且旧程序及数据可恢复。
- [ ] 完成 Windows/macOS 新版本启动失败回退的完整矩阵。Windows 装载失败新增证据见下一条；安装链中的崩溃、挂起和恢复自身失败仍未收口，macOS 暂缓。
- [x] Windows 独立恢复启动器和实际装载失败回退（`89311a8`）；崩溃/挂起的监控子进程测试通过，但完整安装链中的崩溃、挂起以及回退自身失败注入继续未测。就绪后的一般运行期崩溃不属于此启动监控范围。
- [ ] 用实际旧版与原生安装程序完成数据副本双向读写和回退：保留未知字段、裁剪/缩放/位置及迁移后新截图。已有合成数据与 legacy Rust 读取器测试不能代替安装版 GUI 演练。
- [ ] 选择可用于递增升级的候选版本，重新生成双平台、匹配身份的构建/资源收据和产物清单，运行候选工作流并完成真实更新签名验证。当前同为 2.6.0 的包不能证明版本递增更新成功。
- [ ] 联调 GitHub/Gitee 元数据、附件字节/签名与源不可用处理；演练最低 OS 路由及旧 macOS 客户端分流，确认不向不兼容系统提供新包。

依据：[候选记录](candidate-status.md)、[更新交接](update-handoff.md)、[更新与恢复验收步骤](validation.md#6-更新安装和恢复演练)。候选工作流已有构建/可选签名能力，但仅上传审查产物；实际签名、远端运行和发布联调不能据 workflow 文件存在记为通过。

### D. 性能基线与资源收敛（P9-01 / V21）

- [ ] 在同一硬件、固定语料/图片和 release 安装产物上重新测旧版与 GPUI；旧版计入 WebView 子进程，冻结预算。已有探索性待机样本不作为性能结论。
- [ ] 补 20 次冷启动、10 次预热后至少 100 次交互、稳定 60 秒后 120 秒待机采样及至少 2 小时常驻曲线。
- [ ] 测搜索/翻译热唤起、查询首批结果、截图首帧、标注帧耗时、OCR 冷热和流式翻译；拆分捕获、纹理上传、推理及 UI 时间。
- [ ] 测双屏/十贴图峰值，以及 100 次截图/创建/关闭后的 CPU、内存、GPU 纹理、句柄和引用回落；修复持续增长、积压或回归。
- [ ] 按 [性能预算](validation.md#4-性能测量与目标) 判定通过：例如待机 CPU ≤ 单逻辑核 0.5%、工具窗口热唤起 p95 ≤ 150ms，并满足相对旧版的约束；未达到优化目标需记录原因。

## 3. 最终工程切换（P9 / G9）

### P9-02：业务与资源目录迁移

- [x] 六个共享 crate 移至根 `crates/`，模型/字体/图标移至根 `assets/`；25 个资源文件移动前后哈希相同。
- [x] 同步 workspace、资源定位、编译嵌入、xtask、测试和 CI 路径；配方目录统一保留 `native/`。`4f1f107`。
- [x] 通过过渡期 workspace 174 项测试、严格原生检查、release/NSIS、资源/OCR及资料兼容核验；其中包含后来退出 workspace 的两项旧适配测试。`4cf6eea`。[目录迁移记录](evidence/windows-root-layout.md)

### P9-03：旧壳与前端清理

- [x] 从根 workspace 移除旧 `rotor` Tauri 包，清除 Cargo.lock 中的 Tauri/Wry/WebView2 依赖；原生构建不再依赖旧文件。`0dd6792`。
- [x] 版本工具改为只读写根 Cargo.toml/Cargo.lock；生产身份兼容测试不再读取旧壳配置。
- [x] 品牌资源复制到 `doc/branding/`，保留共享业务、模型/字体/图标、许可证及历史证据。
- [x] 整理 91 个已跟踪旧文件的逐项清单，并取得用户明确删除确认。
- [x] 实际删除 91 个旧前端、Tauri、Node 配置文件（2026-09-09）；旧公钥兼容证据先迁入保留目录，删除后 workspace 测试通过。[清单](evidence/legacy-removal-plan.md)

### P9-04：构建、发布工具与开发文档

- [x] 原生 CI 移除 legacy Node/Yarn 构建任务，默认 Windows，macOS 仅在明确选择时加入。
- [x] `publish.yml` 改为核对 tag/版本/checkout，复用原生生产候选构建和签名步骤，准备未发布草稿；更新源推广保持独立。
- [x] 附件整理器校验文件集合、哈希、身份、版本、签名清单及重复附件，全部校验后才输出；5 项合成测试通过。密码学验签仍由上游 Rust 签名步骤负责。
- [x] 更新中英文 README、AGENTS、原生开发命令、平台状态、资源/签名/备份恢复说明；workflow YAML 和内嵌 Python 本地解析通过。
- [x] 版本读取和 set-version dry-run 通过；没有实际改版本、打标签或推送。以上工程改造提交为 `0dd6792`。[原生工程记录](evidence/windows-native-workspace.md)
- [ ] 实际运行远端候选/发布工作流，使用真实更新签名密钥完成验证，创建/审查远端草稿并推广正式更新渠道。工作流代码完成不等于已执行或已发布。

### P9-05：依赖、独立构建与产物验证

- [x] 原生 workspace 的格式、check、172 项测试、全 workspace 严格 clippy 通过；生产身份 common 的 14 项测试通过。
- [x] Windows 共享核心依赖边界检查通过，原生 normal/build 依赖闭包不含 Tauri/Wry/WebView2/QuickJS。[工程记录](evidence/windows-native-workspace.md)
- [x] 复制并逐字节核对 137 个原生输入文件，副本不含 package.json/src-tauri，使用新 target 目录；移除 PATH 中全部 Node/Yarn 入口后，离线 check 和完整 debug build 通过。
- [x] 独立副本程序的身份和资源诊断通过，资源定位到副本根 assets，不依赖旧壳生成目录。[独立构建记录](evidence/windows-native-only-2026-09-08.json)
- [x] 当前提交的开发身份 release 在无 Node/Yarn PATH 下通过；故意设置不同 CARGO_TARGET_DIR 时，xtask 仍正确读取自身构建目录，未创建错误输出哨兵目录。
- [x] 当前提交的正式身份 release 通过；两种身份均完成中文/空格路径暂存和 NSIS 包构建。
- [x] 两套暂存各 31 个清单文件、包哈希、安装器 VERSIONINFO、身份/资源诊断通过；运行库仅 DirectML.dll，无 Tauri/WebView DLL。源码为 `0dd6792`，核验记录提交为 `3a07780`。[候选记录](evidence/windows-native-cutover-candidates-2026-09-08.json)
- [x] 旧文件物理清理后核对原生 workspace 编译、测试、严格 clippy、字体与核心依赖边界（2026-09-09）。macOS 构建/打包及目标系统运行验收仍暂缓。

### P9-06：功能表、证据与最终收口

- [x] 回填 44 项功能的实现位置、主要 commit、证据和行为边界；修正 integration/platform 及根目录迁移后的实际源码链接。`90b7c3a`、`4f1f107`。[功能对照](feature-parity.md)
- [x] 保存各批候选版本、源码/资源摘要、程序与安装包哈希、身份和限制；最新双身份包的构建基准为 `0dd6792`。[候选状态](candidate-status.md)
- [x] 补齐可操作的原生构建、签名、资料导入/备份/恢复及静默安装检查说明。[分发说明](../../native/README.md)
- [x] 保留已有 UI、自动保存、翻译流、搜索释放、快捷键恢复、目录迁移、静默安装及无 Node 构建证据；明确区分通过、代码完成、跳过、暂缓和受阻。
- [ ] 完成旧文件清理、剩余非可视化安装/更新/恢复及性能要求，收口支持系统和升级兼容矩阵，之后才判定 G8/G9 与正式渠道切换完成。

## 4. 后续执行顺序

1. 可视化测试保持跳过，macOS 保持暂缓，不再等待人工浮窗输入。
2. 旧文件清理已完成；持续保留业务、资源、许可证和历史证据。
3. 推进正式身份/递增升级、自启动清理、其余非可视化恢复、实际签名/远端 CI 及性能验证。
4. 核实最终工程和产物，完成发布与更新源推广后再关闭完整迁移目标。

本表按当前证据勾选已完成工作；完整历史见 [progress.md](progress.md)，各项验收方法和预算见 [validation.md](validation.md)。
