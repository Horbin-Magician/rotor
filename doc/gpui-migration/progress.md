# 迁移实施进度

当前分支：`refactor/gpui`。按 2026-09-07 用户指令持续推进实现并分步提交；尚缺的跨平台实机证据不阻断可逆开发，但不据此标记 G0–G9 验收通过或发布正式更新。

## 已提交

- `3b5be7a`：统一有界后台服务和 GPUI 事件桥，49 项核心/画布测试通过。

- `8c75c47`：资源定位与首次使用后数据路径冻结。

- `e0010f9`：类型化搜索请求/索引状态与线程退出，17 项搜索测试通过。

- `1c4a797`：P1 旧壳适配迁出共享核心，30 项核心测试与两端依赖边界检查通过。

- `0c6e182`：P1 根 workspace、正式 native/ui/canvas 入口和双壳 CI。

- `57c1f6a`：P1 配置事务和数据目录隔离。
- `25f2b56`：P0 独立原型、依赖锁定、Windows 实机证据、试验打包和采样工具。跨平台/完整基线缺口见 dependency-baseline.md。

## P1 数据服务

- 新增 `ROTOR_DATA_DIR` 与启动前一次性路径注入；显式空路径不会落回真实用户目录。
- `ConfigService::load_from` 可使用独立数据副本，损坏文件加载失败，不静默覆盖。
- 配置先写同目录临时文件并 flush/rename，成功后才更新内存；多键修改一起提交，未知键保留。
- 旧 `AppConfig` API 继续可用；读取失败的全局实例禁止后续写入，避免以默认值覆盖损坏数据。
- 验证：`cargo test --manifest-path src-tauri/Cargo.toml -p rotor-common --offline`，6 项通过（包括写入失败回滚、损坏文件保留、未知键往返和路径隔离）。旧依赖版本没有升级，锁文件只增加已有 tempfile 的测试引用。

下一步：根 workspace、正式 desktop/ui/canvas crate；随后逐项迁出旧壳适配，接入原生窗口和业务服务。P1 完成门槛尚未通过。

## P1 根 workspace

- 根 workspace 包含 10 个成员，default-members 为 rotor-desktop；新增 rotor-ui、rotor-canvas。P0 experiment 保持独立排除。
- 旧 src-tauri/Cargo.lock 迁至根，旧 release profile 原样上移，旧 manifest/前端/Tauri CLI 入口保留。构建产物改为根 target，旧发布 CI 缓存同步更新。
- 从旧锁文件解析，补齐缺失缓存后保留绝大多数旧版本；必要变化集中在 futures 家族（GPUI 要求至少 0.3.32，配套解析 0.3.34）、regex-automata（新 globset 要求 0.4.18）以及新增 Linux 桌面依赖带来的 zbus_names/zvariant 家族。没有进行全量 cargo update。
- 正式原生入口先接真实 ConfigService 读取；默认使用 home/.rotor-gpui，ROTOR_DATA_DIR 可指定隔离副本；--check-config 只输出路径/键数量，不输出凭据。
- 当前原生设置视图只显示基础配置快照，不是 P3 完整设置交付。
- 验证：native check、3 个新增 crate 的 clippy -D warnings、common/canvas 共 7 项测试、旧 rotor check 全部通过。旧入口现有 Windows setup 的 unused app 警告记录保留。
- 本机预装 stable 实际为 rustc 1.97.0，验证使用 cargo +stable；仓库和 CI 固定命名工具链 1.97.0。

开发入口：在根运行 cargo run -p rotor-desktop（原生），旧版仍使用 yarn tauri dev；P0 则在 experiments/gpui-probe 独立运行。不得把基础窗口当成已完成的功能迁移。

## P1 旧壳适配与核心边界

- 旧 application/tray/screenshot_data、搜索/翻译窗口和截图窗口适配集中到 src-tauri/src/integration；IPC 命令名、事件名、payload 和快捷键语义保持。
- 搜索器保留后台索引/查询核心，旧窗口包装委托核心；截图捕获、缓存、记录、OCR 和会话恢复判断仍在截图 crate；翻译引擎独立，模拟复制下沉 rotor-platform。
- rotor-runtime 的快捷动作解析改用与原插件同版本的独立 global-hotkey 类型；不依赖 Tauri 插件。
- 全部共享核心 crate 已移除 Tauri/GPUI/WebView normal/build 依赖，Windows/macOS 两个目标闭包已通过脚本检查，已接入 CI。
- 验证：旧 rotor check 通过；runtime 2、screenshot 7、searcher 12、translator 9，共 30 项核心单测通过。所有测试使用隔离数据目录。
- 正式 native release 构建通过，--check-config 成功读取隔离 ConfigService。窗口创建及可访问性树可见，但桌面已锁定，视觉/交互复测暂未完成；继续命令行与代码实现。

下一步补齐类型化请求、过期结果隔离、后台任务生命周期和 GPUI 事件桥，再进入完整原生桌面服务和功能界面。

## P1 搜索请求身份与退出

- 每次查询生成进程内唯一 QueryId，批次携带原始 ID；相同查询字符串和不同服务实例也不复用身份。
- 索引状态改为类型化 IndexState，序列化保留旧 JSON 的 unbuilt/unavailable 等值；旧适配继续发送原有字符串事件和结果 tuple。
- 关闭服务后拒绝新查询，Drop/显式 shutdown 发送终止消息；修复接收端断开后原事件循环持续空转的问题。
- 验证：搜索器 17 项测试通过，覆盖重复查询身份、实例间 ID、停止后拒绝、断开后线程退出、旧 JSON 名称；旧 Tauri check 通过。

## P1 资源与数据路径稳定性

- 数据目录在首次解析后冻结，阻止配置、索引和截图在运行中因环境变量/工作目录变化而分流到不同目录。
- ResourceLocator 支持 Windows 同目录 assets、macOS Contents/Resources/assets，以及显式 ROTOR_RESOURCE_DIR；开发源资源回退仅用于 debug。
- 资源解析拒绝父目录/绝对路径逃逸和越界符号链接；显式覆盖无效时返回错误，不静默读取其他资源。
- 9 项 common 测试通过，包括目录冻结、两种安装布局和无效覆盖；新旧入口 check 通过。

## P1 后台服务与原生事件桥

- Services 提供有界事件总线、类型化搜索/翻译/截图/OCR 结果、请求身份、取消及后台并发限制。业务对象不持有 GPUI/Tauri 窗口。
- 配置使用串行队列，磁盘提交后发布快照；UI 读取已提交快照，不等待配置磁盘写锁。退出停止接收请求，关闭发布通道并限时排空已接受的配置写入。
- 旧引擎入口保留，新引擎可注入独立配置；错误对原始和 URL 编码的凭据脱敏。
- 原生入口接 Services，弱引用视图接收事件，主题按钮通过后台队列保存并更新主题；--no-index 用于不扫描磁盘的 UI 验证，release 开发运行可用 ROTOR_RESOURCE_DIR 指定源资源。
- 验证：49 项共享核心/画布单测通过，包含本地 HTTP 翻译、请求 ID、队列容量、关闭时配置顺序落盘；新入口 release、旧入口 check 及新代码 clippy --no-deps -D warnings 通过。
- clippy 明确只约束选定 crate，避免将既有 ntfs_file_map 的历史 question_mark 风格提示混入这一步；历史 lint 留待收敛阶段。
- 用户解锁后，本轮桌面自动化被物理 Escape 停止；不继续桌面操作，原生异步主题交互的实机验收仍待补齐。代码和命令行验证继续。

下一步 P2：单实例激活、托盘/全局热键、窗口注册表与退出流程，然后补齐各功能的原生视图。

## P2 单实例基础

- 使用 Rust 标准文件锁按数据目录区分实例；再次启动只向 loopback 激活端点发送带随机令牌的激活请求，不暴露任意命令或数据接口。
- 元数据大小、地址范围、令牌长度和 socket 等待有边界；退出唤醒阻塞 accept 并回收线程，锁文件不删除，避免 inode 替换导致双主。
- 配置及激活元数据使用私有原子写入；Unix 为 owner-only，Windows 继承数据目录访问控制。
- Windows 的两个单实例单测通过：转发/重新获取、拒绝错误令牌、空闲客户端不阻塞退出；common 既有测试通过。尚未以桌面自动化执行多进程窗口激活。

## P2 原生常驻入口

- 将按数据目录隔离的单实例激活接入正式 native 入口；托盘、开发热键和第二次启动统一唤起设置窗口，窗口注册表清除已关闭句柄并支持重建。
- QuitMode::Explicit 保持无窗口常驻；托盘菜单支持配置语言，开发热键为 Ctrl+Alt+Shift+G，--no-hotkeys 可禁用，--background 可无窗口启动。业务热键随后随对应视图接入。
- 桌面事件采用容量为 1 的合并唤醒通道，退出意图优先且不会因连续激活丢失；应用退出订阅先停止事件、注销热键并关闭服务，实例锁在服务回收后释放。
- 验证：cargo +1.97.0 check -p rotor-desktop、桌面事件单测及 desktop/ui 的 clippy --no-deps --all-targets -D warnings 通过。工具链更新期间短暂缺失编译器，恢复后已使用仓库固定的 1.97.0 重新检查。
- G2 尚未通过：桌面交互循环、macOS Dock/焦点、业务热键事务、剪贴板/对话框/自启动等仍需后续实现及验收。

## P3 基础设置视图

- 原生设置分为通用、搜索、贴图和翻译：语言/主题枚举兼容，支持排除目录、索引状态/重建/释放、保存目录及保存行为、缩放步长、三种翻译引擎和模型/密钥/目标语言设置。
- 输入草稿与已提交快照分开，保存通过串行事务，失败保留草稿；密钥输入遮蔽，排除目录提交成功后重建索引。
- 中英文切换同步设置文案、已打开标题和托盘菜单；保存目录使用 GPUI 原生目录对话框，取消不修改，窗口关闭后的回调通过弱引用失效。
- native check 与 desktop/ui clippy --no-deps --all-targets -D warnings 通过。桌面自动化仍暂停，尚无这一步的视觉/输入/目录选择验收；G3 未完成，概览、完整快捷键录入和快捷操作等继续迁移。

## P4 输入翻译视图

- 新增原生多行输入翻译、三种引擎的结果/流式增量、停止和文本复制。Enter 提交、Shift+Enter 换行；通过公开输入处理接口检查 IME marked range，组合期间不提交或关闭窗口。
- 窗口失焦/关闭取消所属请求，事件按 OperationId 匹配；新的视图不会接收已关闭视图的旧流。
- 托盘增加输入翻译，开发热键 Ctrl+Alt+Shift+W；类型化窗口注册表区分设置/翻译。合并事件队列保留不同窗口的唤起请求，退出优先。
- native check、两项桌面事件单测与 desktop/ui clippy 通过；仅代码验证 IME 防护，真实候选确认/失焦/连续唤起未验收。光标附近位置、划词翻译、动态高度仍待接入，G4 尚未通过。

## P4 搜索打开适配修正

- Windows 管理员打开改用 ShellExecuteW 的 runas verb，文件路径不再拼接到 PowerShell 代码；检查原生启动错误并拒绝 NUL。
- 平台 crate 和旧 rotor check 通过；保留旧入口 3 项已记录警告。本轮未触发真实 UAC/文件启动交互。

## P4 原生文件搜索

- 接入搜索输入、虚拟结果列表、文件图标、上下键/Enter、Escape/失焦关闭、分页、打开文件/目录和 Windows 管理员打开；托盘和 Ctrl+Alt+Shift+F 开发热键可唤起。
- 展示模型按 QueryId 和原始查询匹配，分页按路径去重，结果及图标缓存最多保留 100 项；新窗口先重置服务的分页查询，避免重开相同查询从旧页继续。
- 文件打开通过后台 Services 发布类型化完成事件；启动失败保留窗口并显示错误，成功关闭。
- 9 项 desktop/runtime/ui 单测和对应 clippy --no-deps -D warnings 通过，覆盖重复文本旧请求、分页去重和容量边界。测试发现 Windows HTTP fixture 接收 socket 继承非阻塞模式，已显式切换为带超时的阻塞读写。
- G4 仍未通过：实际索引/IME/打开/分页 UI 验收、动态高度、光标定位和划词翻译继续补齐。

## P4 划词复制前置检查

- Windows/macOS 等待修饰键释放失败或超时后返回错误，不继续发送复制键；Windows 同时检查左右 Win 键。macOS 无法读取键盘状态时不再视作已释放。
- Windows 使用系统剪贴板序列号检测复制，支持再次复制与原剪贴板相同的文本；旧壳也复用此平台能力。
- 两项平台逻辑测试通过（已释放、超时、读取失败）；未执行真实模拟按键。

## P4 原生划词翻译

- Ctrl+Alt+Shift+D 开发热键触发后台划词捕获，先等待修饰键释放、读取稳定剪贴板快照并模拟复制，得到文本后才显示翻译窗。搜索/输入翻译/设置唤起会取消待处理划词结果。
- 剪贴板操作串行，取消已发送的复制仍给予有界恢复机会；支持恢复文本或图片，识别捕获期间再次变化并避免覆盖。备份阶段持续变化时在复制前失败，原生剪贴板占用/错误返回给设置视图。
- 仍保留兼容边界：文本/图片之外的剪贴板格式不保证恢复，多格式内容不保证完整保留；晚于 700ms 的源应用复制也可能超出捕获窗口，不能宣称完整系统剪贴板事务。
- 两项 clipboard 逻辑测试、native check、desktop/ui/runtime clippy 与旧 rotor check 通过。测试没有操作系统剪贴板或发送模拟键；真实捕获/恢复、权限拒绝和跨窗口交互仍待验收。

## P4 工具窗口位置和高度

- 搜索/翻译改用原生 popup 窗口；搜索在鼠标所在屏幕居中，翻译在光标附近并限制在工作区内。Windows 使用目标显示器 DPI 和 GPUI 固定版本的原生显示器标识，避免用主屏缩放解释副屏坐标。
- 搜索窗按可见结果行数调整高度，最多显示 6 行后滚动；再次唤起输入翻译清空旧输入、取消旧流并聚焦输入框。
- desktop/ui clippy 通过，完整 debug 可执行文件构建通过（链接器输出生成 import library 的信息性 warning）；隔离目录 --check-config 成功，损坏 TOML 返回 1 且文件哈希不变。
- 窗口位置/动态尺寸仍待实机验证，尤其混合 DPI、负坐标、macOS 坐标和 popup 焦点；不据编译结果关闭这些 G2/G4 门槛。

## P5 会话和源像素选区

- 新增 UI 无关的原生截图会话状态：捕获完成必须匹配当前代数，取消/新请求使旧结果失效，选区消费验证显示器且只成功一次，恢复要求完整拓扑（包括 DPI）一致。
- 画布坐标显式区分逻辑点和源图像像素，拖动方向归一化、分数缩放、边界裁剪和非有限坐标均有定义；零面积不会导出。
- screenshot/canvas 共 12 项测试通过，包含取消后迟到结果、重复消费、重复显示器和 DPI 变化、反向拖动与图像边界。下一步接原生遮罩窗口和贴图。

## P5/P6 原生贴图记录基础

- 新增显式目录 PinStore，兼容 workspaces/default/shotters 和既有 ShotterConfig；未知顶层字段、工作区和贴图字段保留，缺省 image_rect 可恢复。
- 新 PNG 使用 create_new 并同步完成后发布记录；记录原子写入成功才更新内存，失败只清理这次新图。更新验证裁剪/缩放，删除先提交记录再清理图像，避免写失败破坏可恢复数据。
- 损坏 TOML 返回错误且不覆盖；无效/缺失图像报告恢复警告，保留原记录供恢复处理。
- 三项存储测试通过：透明 PNG/旧结构往返和未知字段、清空可选裁剪、元数据失败回滚、损坏文件保留。此服务尚待接入原生贴图生命周期和退出刷新，G6 未完成。

## P6 贴图后台队列

- Services 提供恢复、新建、更新、删除及 flush barrier；容量为 8 的串行队列保证记录操作按提交顺序执行，磁盘结果以类型化 PinEvent 返回。
- PinStore 的路径来自显式 ConfigService，服务测试不读取真实用户贴图。配置和贴图各保留一个后台写入执行位置，退出关闭事件发布后限时排空已接受写入。
- 后台写入任务异常也会返回失败事件，后续命令明确失败，不让窗口无限等待。
- runtime 7 项测试及 desktop/ui/runtime clippy 通过，包括无人消费事件时退出仍保存新贴图、更新/删除顺序和 flush 后落盘。

## P5/P6 遮罩组件和导出服务

- 新增原始图像上传、遮罩、自动窗口框选、拖动、尺寸、逐像素放大镜、C 取色和 Escape 取消组件；源 RGBA 留给导出，上传副本转为 straight BGRA，不经过 PNG/IPC。
- 捕获批次携带窗口矩形；Windows 新捕获前等待桌面合成。新增借用原生句柄的同步隐藏和客户端物理边界拟合，准备接入主窗口流程，避免 GPUI 异步销毁残留在截图中。
- 贴图队列支持按源图裁剪保存/复制，导出成功后删除记录；失败保留记录。无持久化 ID 的临时图像也可导出，便于存储失败时保留用户图像。
- runtime/ui 共 12 项测试及 clippy 通过；验证 straight BGRA 不改变 RGBA/alpha、负坐标框选裁剪、导出失败保留和成功删除。原生入口 check 通过。
- 此提交是组件/服务准备，遮罩和贴图尚未注册到主窗口流程；下一步接快捷键、会话、恢复和原生贴图窗口。macOS 原生隐藏代码尚未在 macOS 编译或运行。

## P6 旧贴图来源坐标兼容修正

- 进一步核对 PinImageLoad 和旧 Pin.vue 后，修正早期 PinStore 对 image_rect 的解释：它是已裁剪 PNG 在原显示器图像中的来源范围，rect 才是当前可见选区。PNG 内裁剪偏移必须减去 image_rect 的来源原点。
- 没有 image_rect 的旧记录仍按完整显示器 PNG 处理；读取校验 PNG 实际尺寸，原生渲染/导出共享同一个来源坐标转换。
- 新增非零来源坐标、后续边缘裁剪和旧整屏 PNG 测试；PinStore 5 项、runtime 8 项测试通过。后续截图创建必须持久化选区 PNG，不将整屏 PNG 错标为已裁剪图。

## P5/P6 原生截图到贴图链路

- Ctrl+Alt+Shift+S 开发热键和托盘截图接入会话、后台捕获/上传、多屏遮罩；先同步隐藏旧遮罩，再等待合成，避免异步销毁窗口出现在新截图中。几何/DPI 改变时失效，不接收旧代数结果。
- 确认选区后串行队列裁剪并持久化选区 PNG，image_rect 保留来源坐标；退出仍处理已接受的确认操作。失败时准备临时贴图，用户仍可保存/复制。
- 新增原生置顶贴图、拖动/缩放、配置中的局部键、原生保存框、保存目录偏好、成功导出后删除/关闭、失败保留；隐藏和托盘“显示贴图”、启动恢复已接入。
- 贴图准备/恢复在后台进行；截图会话中延后显示，打开其他工具取消未完成遮罩。窗口拟合借用原生句柄；跨屏位置仍保存为旧 monitor_pos + rect + offset 形式，来源 PNG 尺寸不随目标屏幕改变。
- 32 项 canvas/desktop/runtime/screenshot/ui 测试、desktop/ui/runtime clippy 与完整 native debug 构建通过。测试覆盖确认截图后无事件消费者退出仍保存裁剪 PNG；没有运行实际桌面操作。
- G5/G6 尚未通过：真实遮罩焦点/拖动/保存框、多屏/DPI/热插拔、标注/边缘裁剪/OCR、未提交的视图位置在拥塞队列中的退出刷新和性能指标仍需继续完成。

## P6 共享标注文档和合成

- rotor-canvas 新增源图坐标文档、画笔/矩形/箭头/文字、裁剪、撤销/重做和视口映射；无效编辑不改变历史或版本。
- resvg 在指定输出物理像素尺寸重新栅格化矢量，底图用预乘 alpha 采样；未缩放且未覆盖的源 RGBA（包括透明像素 RGB）保持不变。临时标注层仅分配实际绘制包围框。
- 固定 Noto Sans CJK SC Regular 2.004 字体，保留版权、OFL 许可证、来源提交和 SHA-256；字体资源约 15.7 MiB，供文字功能延迟加载，并允许系统字形回退。CI 验证字体/许可证校验和。
- 10 项 canvas 测试及 clippy 通过，覆盖 alpha、透明边缘、不同输出尺寸的矢量清晰度、中文和 XML 字面量、撤销与分数缩放。Windows/macOS 依赖图均通过纯核心边界检查（不是 macOS 编译验收）。
- 此步尚未将标注控件接入 PinView；现有原生源尺寸导出将改为该显式输出尺寸接口，以承接旧 Stage × pixelRatio 语义，最终仍须实测旧 zoom/DPI 矩阵。

## P6 原生标注和视口导出

- PinView 接入画笔/矩形/箭头/中文文字、撤销/重做和文字完成/取消；文字编辑及 IME 组合优先消费按键，不触发贴图保存/关闭。失焦取消未完成的指针笔画。
- 绘制时使用即时路径预览，完成后后台产生当前视口物理尺寸的 RGBA 帧；屏幕使用该帧，保存/复制直接导出同一帧，不再误套来源 PNG 的裁剪坐标。
- 窗口逻辑尺寸按来源屏幕缩放与 zoom 计算，当前 DPI 参与输出；连续缩放合并渲染请求，旧 epoch 结果被丢弃。渲染失败回退编辑并保留重做，不能导出旧尺寸。
- 合成请求异步等待容量，关闭服务会唤醒等待者；字体后台注册到原生输入系统，文字合成首次使用时加载。临时框架控件不会进入导出像素。
- native check/build 和 clippy 通过；canvas 10、runtime 11、ui 7、desktop 2 项相关测试通过（分批运行）。新增导出像素/视口尺寸一致、容量等待关闭唤醒及过期/失败帧状态测试。
- 尚未做真实绘图、中文组合键、不同 DPI/zoom 的旧版对照；边缘裁剪、OCR 及退出时未入队位置刷新继续实现，G6 保持未通过。

## P6 八方向边缘裁剪

- 新增源坐标裁剪算法，八方向固定对边、12 像素最小值和 PNG 边界约束；小于最小值的原图不会被扩成越界选区。
- 原生边缘拖动同步客户端位置/尺寸，拖动中只预览，结束时一次提交历史和记录；Escape 恢复开始状态，撤销/重做同步窗口，DPI 改变时结束当前拖动。
- Windows 增加操作系统指针捕获并与 GPUI 命中捕获配合，避免指针离开窗口后丢失操作；macOS 使用借用 NSView/NSWindow 的几何转换（仍待 macOS 编译和实测）。
- 小于 24 逻辑像素的贴图优先整体拖动；边缘显示方向光标。扩展裁剪时底图补齐未合成区域，透明像素不重复叠加。
- canvas 12、ui 7 项测试及 native/core clippy 通过，包含极端拖动、全部方向、微小源图和裁剪历史。真实拖动/跨屏/UAC/IME 验收未执行，G6 未通过。

## P6 退出最终记录

- 退出同步收集视图的已提交记录（不包含未结束的裁剪预览），交给独立最终快照；不受普通命令队列容量影响。
- 后台队列排空后批量原子提交仍存在的贴图，跳过已删除记录，避免保存/关闭与退出竞争导致贴图复活。已有运行时限时回收继续负责等待。
- 该路径不依赖 GPUI 只有 200ms 的 quit future 时限，也不在 UI 线程执行磁盘写入。
- runtime/screenshot 共 28 项测试及 clippy 通过，包含命令/事件双队列已满时最终位置落盘、已删除记录不会复活。文件系统本身失败仍记录错误并保留最后有效记录。
